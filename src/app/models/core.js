export default class Core {

  update() {

  }

  getColor() {
    return 0xF8DC9F;
  }

  getDimensions() {
    return [0.5 + (Math.random() - 0.5), 0.5 + (Math.random() - 0.5), 0.5 + (Math.random() - 0.5)];
  }
}
